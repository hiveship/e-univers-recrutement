<% if object && object.errors.any? %>
    <div id="flash" class="container">
        <strong><%= pluralize object.errors.count, 'error' %> prohibited
            this <%= object.class.to_s.downcase %> from being saved :</strong>
        <ul>
            <% object.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
        </ul>
    </div>
<% end %>
